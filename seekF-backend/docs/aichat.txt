1. 模型层（单例池）
deepseek → 全局单例
qwen → 全局单例
glm → 全局单例
只负责推理，不存任何用户数据！
2. 会话层（每个会话一个）
从数据库读取历史
调用对应的模型单例
把新消息存回数据库
不做成单例
不长期驻内存
3. 存储层（数据库Mysql / Redis）
所有聊天记录存在这里绝对不放在结构体里！

消息通过Kafka异步持久化，避免阻塞。流式响应用SSE推送实时内容。
参考本项目原有风格，结合eino框架。实现aichat的后端代码。